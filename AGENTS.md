# AGENTS.md

本文件是本仓库中 AI 编码助手的统一工作约定。规则适用于整个仓库；子目录若存在更具体的 `AGENTS.md`，则以距离目标文件最近的规则为准。

## 项目概况

本项目是一个面向《永远的蔚蓝星球》微信小游戏的 Windows 本地自动化工具，计划使用 Python 3.10+、OpenCV、MSS、pywin32 和 PyAutoGUI 实现窗口截图、图像识别、任务决策与安全输入。

项目坚持低侵入原则：不修改游戏文件、不注入进程、不读取或分析网络数据，也不绕过验证码、登录验证、风控或其他平台安全机制。

## 开始工作前

1. 阅读 `README.md` 了解项目目标、技术栈和计划目录结构。
2. 根据任务阅读对应设计文档：
   - `docs/architecture.md`：架构、模块职责、坐标体系和技术约束。
   - `docs/game-rules.md`：已经确认的游戏规则和业务概念。
   - `docs/automation-flow.md`：自动化状态机及完整执行流程。
   - `docs/todo.md`：跨任务的基础设施、运行环境和通用工具待办。
   - `docs/coop/TODO.md`：合作主流程尚未确认的参数、策略和素材；不得把其中内容当作已确认事实。
   - `docs/logging.md`：日志级别、`--debug` 开关和必须打日志的位置。
3. 先检查工作区现状和已有改动，不覆盖或回退用户的无关修改。

## 事实来源与文档维护

- 已确认的玩法规则写入 `docs/game-rules.md`。
- 自动化步骤、状态迁移和失败分支写入 `docs/automation-flow.md`。
- 架构职责、依赖方向、坐标与分辨率策略写入 `docs/architecture.md`。
- 跨任务待办写入 `docs/todo.md`；合作主流程的未确认数值、界面特征、素材需求和待定策略写入 `docs/coop/TODO.md`。
- 日志级别、`--debug` 开关和必须打日志的位置写入 `docs/logging.md`。
- 实现改变了公开用法、配置或目录结构时，同步更新 `README.md`。
- 不猜测截图中无法验证的状态、坐标、阈值或游戏规则；缺少证据时保守停止，并记录为 TODO。

## 架构约束

- 保持 `CLI -> Runner -> Task Engine -> Perception/Action` 的依赖方向。
- 可独立验证且会产生输入的业务能力统一挂到 `exec` 命令组，并复用 Runner、任务决策和 Action Executor；禁止为同一能力新增顶层命令、在 CLI 复制业务流程或绕过 Safety Guard。
- 任务状态机只依赖识别结果和动作执行接口，不直接调用 OpenCV、MSS、pywin32 或 PyAutoGUI。
- `Vision` 只负责识别；ROI、坐标换算和动作点计算统一由 `Locator` 负责。
- `Input Controller` 只能通过 `Action Executor` 使用，任何任务都不得绕过 `Safety Guard` 直接输入。
- 截图、识别和输入必须共享同一个有效的 `WindowContext`；关键动作前重新校验窗口句柄、客户区、DPI 和 `frame_id`。
- 内部坐标统一使用窗口客户区物理像素，仅在实际输入前转换为屏幕坐标。
- 游戏客户区布局固定为 9:16；所有业务 hotspot、ROI 和棋盘定位参数必须以客户区比例坐标写入可提交的 `configs/tasks.yaml`，运行时再换算为客户区物理像素。禁止把业务坐标写入 `configs/default.yaml`、`configs/local.yaml` 或任务代码；`configs/local.yaml` 只允许保存本机窗口规格和 `window.template_pack` 覆盖。
- 模板包默认按游戏窗口所在显示器的物理分辨率选择，允许 `window.template_pack` 显式覆盖；禁止按主显示器、客户区尺寸或其他分辨率回退。
- 业务任务中避免硬编码屏幕绝对坐标、识别阈值、延迟和重试次数；放入配置或分辨率素材包。
- 每个输入动作都必须有前置状态判断和动作后验证。识别失败、窗口变化或状态不确定时暂停或退出，禁止盲点、无限重试。
- `Debug Recorder` 只记录原始截图、标注图、识别结果和动作事件，不参与业务决策。

## Python 代码约定

- 目标运行环境为 Python 3.10+；新增公共接口和核心领域模型应添加类型标注。
- 优先使用小而明确的模块、数据类或 Pydantic 模型，避免在任务代码中传递无结构字典。
- 路径使用 `pathlib.Path`；文件和配置默认使用 UTF-8。
- 日志应包含任务、状态、`frame_id`、置信度及失败原因，但不得记录账号、令牌或其他敏感信息。
- 捕获具体异常并保留上下文；不要用空的 `except` 或静默忽略自动化失败。
- 随机延迟、超时和重试必须可配置、可测试，并设置明确上限。

## 日志约定

详细级别定义、`--debug` 用法、命名空间规范和必须打日志的位置见 [docs/logging.md](docs/logging.md)。核心规则：

- 所有日志器通过 `get_logger(__name__)` 获取，统一挂在 `wlxq_bot` 命名空间下；禁止裸 `logging.getLogger`。
- 日志分 `ERROR`/`WARNING`/`INFO`/`DEBUG` 四级；`--debug` 切到 DEBUG，默认 INFO。
- 每个执行类命令（`screenshot`/`run`/`click`/`spam-click`/`move` 等）的关键节点都要打日志：开始/结束用 INFO，窗口句柄/坐标换算/`frame_id` 等诊断数据用 DEBUG，失败用 ERROR/WARNING；高频循环不在循环内打。
- `logger` 记录过程诊断，`rprint` 输出用户面向结果，职责不互相替代。

## 测试与验证

- 纯决策逻辑使用单元测试覆盖，尤其是状态迁移、停止条件、召唤/合成策略和配置校验。
- 图像识别测试优先使用固定截图和模板，不依赖实时游戏窗口。
- 输入控制默认使用 mock/fake，自动化测试不得真实点击用户桌面。
- 修复缺陷时添加能复现问题的回归测试。
- 提交前运行与改动相关的测试；具备完整测试套件后使用 `python -m pytest`。
- 仅修改 Markdown 时至少检查链接、术语及各文档之间是否一致。

## 变更原则

- 以完成当前任务所需的最小改动为主，不顺手重构无关代码。
- 不提交运行生成物、真实账号截图、调试截图、缓存、虚拟环境或敏感配置；需要示例素材时先脱敏。
- 不删除或改写用户提供的原始截图和模板，除非任务明确要求。
- 新增依赖前说明用途，并同步维护依赖清单和安装文档。
- 不声称未运行的测试已经通过；受 Windows、微信窗口或素材限制而无法验证时，明确说明未验证项。

## 完成标准

任务完成时应满足：实现符合现有设计文档；相关测试或检查已经执行；失败路径保持保守和可观察；配置、文档及测试随行为变化同步更新；最终说明改动内容、验证结果与仍存在的限制。
