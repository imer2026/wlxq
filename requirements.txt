# 运行时依赖，与 pyproject.toml [project].dependencies 保持一致
# 开发依赖见 pyproject.toml [project.optional-dependencies].dev
typer>=0.12
pydantic>=2.6
pyyaml>=6.0
opencv-python>=4.9
numpy>=1.26
mss>=9.0
pillow>=10.0
pywin32>=306
pyautogui>=0.9.54
rich>=13.7
# 技能标题识别（选卡优先级）与离线建册 OCR
rapidocr-onnxruntime>=1.3
