# 项目 TODO

本文档只记录跨任务的基础设施、运行环境和通用工具待办。合作模式端到端流程的策略、素材和状态机待办统一维护在 [合作主流程 TODO](coop/TODO.md) 中。

已确认的游戏事实写入 [游戏规则知识库](game-rules.md)，已确定的自动化步骤写入 [自动化执行流程](automation-flow.md)，已完成事项通过 Git 历史追踪，不再堆积在 TODO 中。

## 运行环境

- [ ] 验证微信小游戏在窗口遮挡、最小化和前后台切换时的截图行为。
- [ ] 验证 PyAutoGUI 点击、拖动和按键在目标窗口中的稳定性；不稳定时评估 `pydirectinput`。
- [ ] 在多显示器、不同 DPI 和窗口跨屏场景验证 `WindowContext` 重校验。
- [ ] 为各显示器物理分辨率采集模板包，并验证模板与该分辨率下的实际截图一致。

## 安全与可观察性

- [ ] 为当前按业务约定立即提交的招募入口、难度勾选、点赞和宝箱点击补充可识别的动作后置条件；缺少实机证据时不得猜测状态或坐标。
- [ ] 实现 `DebugRecorder` 的原始截图、标注图和事件落盘。
- [ ] 检测未知界面、连续识别失败和窗口变化时自动保存脱敏调试证据。
- [ ] 增加人工端到端安全冒烟测试，覆盖窗口调整、截图、识别、输入门禁和停止热键。

## 工具与素材治理

- [ ] 实现交互式 `calibrate`，统一采集模板、ROI、hotspot 和模板包元数据。
- [ ] 建立固定脱敏截图集，覆盖各模板包的离线识别回归测试。
- [ ] 统一模板文件命名，清理仍使用截图工具默认名称的素材。

## OCR 文字识别

2026-08-30 已用 `rapidocr-onnxruntime`（仅装入 venv，未进 requirements.txt）在四类真实素材上完成可行性验证（脚本与标注图在 gitignore 的 `outputs/ocr_poc*.py`/`ocr_*.png`）：底部小数字（3.3K / 红色 3.5K / 45）、难度面板行（「合作模式-彩虹N层」+ 绿勾状态）、技能卡标题与混色描述、三选一技能页整页文字。识别同时返回文字、外接框、置信度，可直接作文字锚点定位。已知边界：OCR 不感知颜色（红/金状态需对识别框做 HSV 像素统计，红金色相分界需按实机素材定阈值）；战斗飘字区域识别混乱，禁止在该区域使用；个别帧小字会漏检，需 ROI 限域 + 放大预处理 + 多帧采信。

- [ ] 新增 `perception/ocr.py`：封装 OCR 引擎（加载、ROI 裁剪、放大预处理），返回结构化结果（文字、框、置信度、解析字段）；只负责识别，坐标换算和动作点计算仍归 `Locator`。
- [ ] `Locator` 增加 `text` 定位策略：`configs/tasks.yaml` 声明 `expected_text`、匹配方式（精确/模糊）、阈值与 ROI，复用 `MatchResult` 与 `action_point` 体系；阈值与匹配参数一律进配置，禁止硬编码。
- [ ] 难度面板识别迁移 OCR：替代 `cai_hong_1~18.png` 文字模板（18→0 张），覆盖普通区第 1~12 层（现无模板）与缺失的彩虹 19 层；滚动策略从盲滚计数改为识别到目标层号即停。前置验证见 [合作主流程 TODO](coop/TODO.md)。
- [ ] 评估文字型界面标志逐个迁移 OCR（准备 / 返回 / 召唤 / 选技能 / 请选择1个额外技能 / 天使20级效果 / 退队 / 双倍奖励 / 取消 / 点击空白处关闭——均已确认为文字模板）：小按钮在真实帧（按压态、置灰态）的识别率需实机验证；必须沿用按状态门控识别，禁止无门控进入高频循环（OCR 单帧全图约 100~300ms）。
- [ ] 资源/进程文字感知新能力（顶部难度层号、波次、金币、强化/召唤费用及红色状态、血量）：各数字的游戏含义先实机确认并写入 [游戏规则知识库](game-rules.md)，再接入任务决策。
- [ ] 建立各场景 OCR 离线回归截图集与置信度阈值基线；OCR 文字置信度与模板匹配置信度语义不同，阈值体系不得混用。
- [x] 技能卡自动采集与离线建册（2026-08-30 实现，2026-09-01 实机归因后两轮重构）：`perception/skill_collector.py` 统计阶段旁路采集——**仅在「页面标志可见 + 技能卡图标已实际命中(即将点卡)」的帧**裁卡（只看状态会裁出棋盘垃圾帧，只看页面标志会裁到卡片/图标未渲染完的过渡帧）、运行时只做裁剪+aHash（毫秒级、默认 30 秒节流）、PNG 编码在写盘线程、异常熔断、不参与决策；`wlxq-bot build-skill-catalog` 离线完成垃圾过滤（卡身浅色占比结构预过滤 + OCR 实义文本校验，过滤结果写入 `filtered.txt` 供复核捞回）、英雄归属（图标模板匹配）、名称模糊合并（OCR 同名异写归并，阈值 0.7）与 OCR，生成/增量合并 `configs/skills.yaml`（技能名+描述，按英雄平铺，不区分开局/赠送页，人工 priority 字段保留）。实机教训：本机生产组合（3000x2000 模板包 × 927x1727 窗口）单帧识别 2.6~6.5s，任何运行时识别增量都会击穿 `safety.frame_ttl_ms` 导致保守停止——采集器因此设计为零识别增量，`frame_ttl_ms` 已实机定案 8000。
- [ ] `rapidocr-onnxruntime` 目前仅装入本地 venv（在线采集与在线决策均不依赖）；待 OCR 正式接入难度面板或界面标志识别（见上）时再写入 `requirements.txt` 并同步 README 安装说明。
- [ ] 技能清单采齐并人工复核后，基于 `configs/skills.yaml` 的 `priority` 字段实现技能选择优先级策略（当前为随机选卡）。
